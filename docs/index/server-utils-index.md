# server/utils — Index

> Current state only. No history. See `docs/index/README.md` for column definitions and maintenance rules.

> **Convention:** Domain `inventory` = utils whose core job is stock/availability logic. Payment/rental utils that merely call inventory deduction downstream do not carry the `inventory` tag.

| File | Responsibility | Domain | Risk | Edit rules | Tests |
|---|---|---|---|---|---|
| `payment-core.ts` | Pure helpers for Omise charge mapping, amount conversion, currency normalisation, and HMAC webhook signature verification — no DB calls. | `payment` | `critical` | Never add a DB call or side effect; do not change `toGatewayAmount` rounding (`Math.round(value * 100)`) without updating every caller that passes amounts to Omise; `verifyOmiseWebhookSignature` uses `timingSafeEqual` — any drift silently accepts or rejects all webhooks. | `payment-core.spec.ts`, `payment-card.spec.ts`, `payments-poll.spec.ts` |
| `payments.ts` | Orchestrates order and `payment_attempts` DB writes after a gateway result; `applyGatewayResult` transitions `orders.payment_status` to `paid` and triggers inventory deduction via the `f_apply_order_inventory` RPC. | `payment` | `critical` | `applyGatewayResult` is the canonical write path for order payment status — never bypass or reorder the `orders.update` → `applyOrderInventory` → cart-clear sequence; `assertGatewayAmountMatches` must remain a hard block before any status transition. | `payment-card.spec.ts`, `payments-poll.spec.ts`, `mixed-checkout-finalization.spec.ts` |
| `omise.ts` | Makes live Omise API calls (card charge, PromptPay source + charge, retrieve, expire) and normalises raw responses into the internal `NormalizedGatewayCharge` type consumed by all payment callers. | `payment` | `critical` | Never pass a raw decimal amount to Omise — all amounts must go through `toGatewayAmount` before the API call; `getOmiseSecretKey` must remain the single access point for the secret key so misconfiguration is always a hard 500, not a silent empty-string auth. | `payment-card.spec.ts`, `mixed-checkout.spec.ts` |
| `mixed-checkout.ts` | Validates cart items, rental bookings, and shipping; computes the allocation plan; creates `mixed_checkout_sessions`, `mixed_payment_attempts`, and `mixed_payment_allocations` rows; fires the Omise charge. | `payment`, `rental-ops` | `critical` | `prevalidateMixedCheckout` is the mandatory gating step — never skip it or change its amount math independently of `assertMixedGatewayAmountMatches` in `mixed-checkout-finalization.ts`; the allocation `amount` written here is what finalization reconciles against the gateway charge. | `mixed-checkout.spec.ts` |
| `mixed-checkout-finalization.ts` | Processes a paid gateway result by finalizing allocations: marks orders as `paid`, confirms rental bookings, deducts inventory, and records held-balance events; raises `critical` alerts on per-allocation failure. | `payment`, `rental-ops` | `critical` | `assertMixedGatewayAmountMatches` must be called before any DB write in `applyMixedCheckoutGatewayResult`; the `finalizing` status guard prevents double-finalization — never remove or weaken it; a per-allocation failure path must emit a `mixed_booking_confirm_failed` alert, never silently swallow it. | `mixed-checkout-finalization.spec.ts` |
| `mixed-checkout-cancellation.ts` | Cancels a `mixed_checkout_session` that has not yet captured payment, voiding only pending/uncaptured allocations (`planned`, `payment_pending`); enforces status guards that block cancellation once any attempt or allocation reaches a paid, finalizing, or review state. | `payment` | `critical` | Narrowing `PAID_OR_REVIEW_STATUSES` to exclude `"finalizing"` opens a race window where gateway-captured funds correspond to voided allocations — silent money corruption with no alert; the `.in("status", [...CANCELLABLE_SESSION_STATUSES])` predicate is the last-resort concurrency guard but must not be relied on alone. | `mixed-checkout.spec.ts` |
| `kyc.ts` | Pure KYC helpers: identity normalisation + HMAC hashing (`hashKycIdentity` is the sole authorised entry point), live readiness computation, pickup-gate resolution (`resolvePickupKyc` + `findPickupOverride`), best-profile selection (`selectBestKycProfile` — the single source of truth for ranking duplicate profiles), §a document-type completeness (`computeKycDocumentCompleteness` — fail-closed on unknown pairs; company requires company_cert + vat_certificate), and cert recency check — no DB calls. | `kyc` | `critical` | `hashKycIdentity` is the ONLY permitted entry point for hashing raw identity input — never call `hashIdentity` on unnormalised values directly; changing the HMAC input format (`v1:${identityType}:${normalizedValue}`) or its separator/ordering orphans every stored `identity_hash` — a deliberate migration plan is required before any such change; identity is stored as hash + `identity_last4` only — never store plaintext; `selectBestKycProfile` must stay pure (no DB) and never decide pickup eligibility — live expiry is `resolvePickupKyc`'s job; error messages must not include PII. | `kyc.spec.ts`, `user-kyc-api.spec.ts`, `company-kyc-api.spec.ts` |
| `kyc-profile-view.ts` | The single PII whitelist boundary for KYC profile API responses: `KYC_PROFILE_SAFE_SELECT` (the only safe column list), `SafeKycProfile` (the only response shape), and `toSafeKycProfile` (the only DB-row→response mapper). Pure — no DB calls. Includes `holderName` (§a name-on-ID / company name — display metadata, never an identity root). Used by KYC profile lookup + create (and later attach/verify). | `kyc` | `high` | The mapper is the PII whitelist — never add `identity_hash`, raw identity, raw `user_id` (expose only `hasUserId`), storage paths, `kyc_documents`, or rejection/revocation/verification-secret fields to `SafeKycProfile`, the SELECT, or the mapper output; every KYC profile endpoint MUST return through `toSafeKycProfile` and never a raw DB row; any field added here is a deliberate privacy decision requiring review. | `kyc-profile-view.spec.ts`, `kyc-profile-lookup-api.spec.ts`, `kyc-profile-create-api.spec.ts` |
| `kyc-documents.ts` | KYC document upload/download helpers for the dedicated private `kyc-profile-documents` bucket: hard streaming body limit (`readRawBodyWithHardLimit` counts actual bytes, never trusts Content-Length), magic-byte MIME sniffing (JPEG full / PNG full 8-byte signature / PDF only — client MIME never trusted), opaque random-UUID storage keys (`buildKycDocumentStorageKey`), fail-closed stored-path validation (`isSafeKycDocumentStoragePath` — extension alternation derived from the same `KYC_DOCUMENT_MIME_EXTENSIONS` map as the builder, regex-escaped), canonical MIME→extension lookup (`kycDocumentExtensionForMime`), strict route-id classification (`asUuidOrNull` — uuid shape or null, for audit-log purity), single-IP x-forwarded-for parsing (`parseSingleForwardedIp`), strict date-only validation (`parseKycDateOnly`, `isFutureKycDate` — Asia/Bangkok), and the append-only `kyc_document_access_log` writer (`logKycDocumentAccess` — payload typed against the generated Insert type; best-effort by default with a console.error breadcrumb, `failClosed` REQUIRED for the allowed-download log written before byte delivery). | `kyc` | `critical` | The access log is immutable and outlives the document — entries must contain ONLY opaque references and non-PII snapshots (never names, identity/document numbers, phones, raw/malformed route ids, unsafe storage paths, or human-readable paths; `ip_address` must be a single validated IP, never a raw header chain); malformed route ids must be classified via `asUuidOrNull` and logged as `document_id = null` + a `*_malformed_*` reason — never the raw string (decisions.md 2026-06-05 Decision G); storage keys must stay random-UUID-only — never derive from profile id, identity value, or filename; the path validator and key builder must keep deriving from the single `KYC_DOCUMENT_MIME_EXTENSIONS` map — never a second hardcoded extension list; the byte-count limit and magic-byte sniff are the upload security gate — never replace them with Content-Length or client-declared MIME; the log payload must stay typed against the generated `kyc_document_access_log` Insert type (no `Record<string, unknown>` casts); allowed-download log calls MUST pass `failClosed: true` and precede the storage fetch. | `kyc-document-upload-utils.spec.ts`, `kyc-document-upload-api.spec.ts`, `kyc-document-upload-h3-integration.spec.ts`, `kyc-document-download-api.spec.ts`, `kyc-document-download-h3-integration.spec.ts` |
| `kyc-document-view.ts` | The single PII/storage whitelist boundary for KYC document API responses: `KYC_DOCUMENT_SAFE_SELECT` (the only safe column list), `SafeKycDocument` (the only response shape), `toSafeKycDocument` (the only DB-row→response mapper), and `KYC_DOCUMENT_DOWNLOAD_INTERNAL_SELECT` (SERVER-INTERNAL select for the proxy download endpoint — includes `storage_path`, must never be serialized to a client). Pure — no DB calls. Used by the KYC document upload and download endpoints (and later list/verify). | `kyc` | `high` | The mapper is the storage/PII whitelist — never add `storage_path`, storage bucket, public/signed URLs, `uploaded_by_user_id`, identity values, or any customer PII to `SafeKycDocument`, the SELECT, or the mapper output; every JSON-returning KYC document endpoint MUST return through `toSafeKycDocument` and never a raw DB row; `KYC_DOCUMENT_DOWNLOAD_INTERNAL_SELECT` rows are server-internal only — the download response is raw bytes with whitelisted headers, and no field of the internal row may reach a response body, header, or error message. | `kyc-document-upload-utils.spec.ts`, `kyc-document-upload-api.spec.ts`, `kyc-document-upload-h3-integration.spec.ts`, `kyc-document-download-api.spec.ts`, `kyc-document-download-h3-integration.spec.ts` |
| `admin.ts` | Exports `requirePlatformAdmin` and `requireSuperAdmin` — the auth guards used by every admin API route; reads `platform_role` from the `users` table via the service-role client. | `auth` | `critical` | `ALLOWED_PLATFORM_ROLES` is the complete set for admin access — do not expand it without a security review; `requireSuperAdmin` must always delegate to `requirePlatformAdmin` first so the role DB read is never bypassed. | `admin-access.spec.ts`, `admin-order-queue-auth.spec.ts` |
| `pos-rental-booking-deposit-finalizer.ts` | Payment-method-agnostic POS Booking Deposit finalization: delegates the ATOMIC money core (held-balance event + deposit-paid fields + attempt→paid + draft→confirmed) to the `f_confirm_rental_booking_deposit` RPC (migration 119), maps its return (`confirmed` / `paid_confirm_failed`) and RAISE codes (via `mapDepositRpcErrorStatus`), then best-effort issues the BDC document (W5) OUTSIDE the money txn. | `pos`, `rental-ops` | `critical` | Money atomicity now lives in the migration-119 RPC — this util must NOT re-introduce separate held-balance/deposit/confirm writes; document issuance (W5) must remain isolated and never revert booking confirmation or payment status; a non-23505 doc-task insert error must surface as `document.status='failed'` (never swallowed — finding 12). | `admin-pos-v3-booking-deposit-payments.spec.ts`, `admin-pos-v3-qr-booking-deposit.spec.ts`, `pos-v3-cash-deposit-crash-gap.spec.ts`, `migration-119-atomic-deposit-confirm.spec.ts` |
| `pos-rental-qr-booking-deposit.ts` | Applies Omise gateway result for POS V3 Booking Deposit QR: enforces the terminal/finalizing state machine, asserts amount/currency match, handles late-payment recovery on locally-expired attempts, then delegates to `finalizePosRentalBookingDeposit`. | `pos`, `payment` | `critical` | Amount/currency assertion (step 2) must run before any DB write that advances attempt status; the `TERMINAL` list controls idempotency — never remove `"paid"` or `"paid_confirm_failed"`; late-payment recovery on `"expired"` + gateway `"paid"` must always emit a `critical` alert before proceeding to finalization. | `admin-pos-v3-qr-booking-deposit.spec.ts`, `admin-pos-v3-qr-webhook.spec.ts` |
| `pos-rental-qr-remaining-deposit.ts` | Applies Omise gateway result for POS V3 remaining security deposit QR: same state machine as `pos-rental-qr-booking-deposit.ts` but records a `remaining_security_deposit_collection` held-balance event and writes `deposit_payment_status = "paid"` — does not call `confirmRentalBooking` or issue a BDC. | `pos`, `payment` | `critical` | `rental_bookings.deposit_payment_method` must use `"qr_transfer"` (the only QR value allowed by the DB CHECK constraint) — never change it to `"promptpay_qr"`, which is only valid in `pos_rental_payment_attempts`; the held-balance event must succeed before the booking update. | `admin-pos-v3-remaining-security-deposit-payments.spec.ts`, `admin-pos-v3-remaining-security-deposit-qr.spec.ts` |
| `rental-booking-deposit-payment.ts` | Customer-facing Booking Deposit payment flow: terms agreement recording, deposit eligibility assertion, payment line computation and storage, gateway result application (`applyRentalBookingDepositGatewayResult` writes deposit paid state and calls `confirmRentalBooking`), and attempt expiry normalisation. | `payment`, `rental-ops` | `critical` | In `applyRentalBookingDepositGatewayResult`, both `assertGatewayAmountMatchesBookingDeposit` and the attempt-amount vs bookingDeposit cross-check must run before any booking field is written; `markBookingDepositConfirmFailed` must always fire a `critical` payment alert — never swallow confirmation failure silently. | `rental-booking-deposit-payment.spec.ts`, `admin-pos-v3-booking-deposit-payments.spec.ts` |
| `rental-payment-lines.ts` | Server-side wrapper around the app-layer `calculateRentalPaymentLines`/`summarizeRentalPaymentLines` utilities; exports `rentalPaymentLineInsertRows` (maps typed lines to DB insert payload) and `isMissingRentalPaymentLinesTable` (error-code detection for schema guard). | `rental-money` | `high` | Column names in `rentalPaymentLineInsertRows` must match the DB schema exactly — `applies_to_security_deposit` and `reduces_remaining_security_deposit` are semantically distinct boolean flags; never swap or collapse them, as downstream money summaries and POS pickup calculations depend on their separation. | `rental-payment-lines.spec.ts`, `rental-booking-deposit-payment.spec.ts` |
| `rental-money-summary.ts` | Aggregates booking snapshot + payment lines into `RentalMoneySummary` (held deposit, pickup due, rental fee, settlement preview) for POS and admin display; also loads the source data from DB. No DB writes. | `rental-money` | `high` | `buildRentalMoneySummary` uses `hasPosV3SameDayPolicy` to gate `pickupRemainingDepositCovered` — misidentifying same-day lines changes the displayed pickup amount; `legacyFallbackUsed` must remain `true` when payment lines are absent so callers detect the limited interpretation and do not treat legacy amounts as authoritative. | `rental-money-summary.spec.ts` |
| `rental-held-balance-events.ts` | Writes and validates held-balance ledger events in `rental_held_balance_events`; `recordRentalHeldBalanceEvent` is idempotent via unique-constraint replay with exact-match validation; `assertBookingDepositHeldBalanceEvent` is the gate that `confirmRentalBooking` calls before transitioning a booking to confirmed. | `rental-money` | `critical` | `assertExistingEventMatches` must check amount, currency, source_type, source_id, booking_id, and status — never relax any comparison; the 23505 idempotency path must always re-validate the existing row before returning it, never short-circuit to a bare reload; `assertBookingDepositHeldBalanceEvent` must throw if the event is missing — never convert it to a warning. | `rental-held-balance-events.spec.ts`, `rental-booking-deposit-payment.spec.ts`, `admin-pos-v3-booking-deposit-payments.spec.ts` |
| `rental-held-balance-summary.ts` | Pure aggregation of held-balance events into `RentalHeldBalanceSummary` (collections incl. T2 `settlement_additional_collection` accumulator, reductions, available balance by bucket); also loads events from DB. No DB writes. | `rental-money` | `med` | `remaining_security_deposit_collection` events must accumulate into `pickupHeldBalanceCollectedAmount`, not `bookingDepositCollectedAmount` — this Phase 2E-B1.5 distinction is load-bearing for settlement bucket accuracy; swapping it would misstate the held-balance breakdown shown in POS settlement. | `rental-held-balance-summary.spec.ts` |
| `rental-booking-confirmation.ts` | Canonical booking confirmation path: loads a fresh snapshot, validates deposit paid + held-balance event, checks dates/duration/branch/pricing/availability, then writes `status = "confirmed"` with a concurrency guard. Used by all payment flows (customer, POS, mixed checkout). | `rental-ops` | `critical` | `validateRentalBookingForConfirmation` must always call `assertBookingDepositHeldBalanceEvent` when `requireBookingDepositHeldBalanceEvent` is set — never skip it; the `.in("status", allowedStatuses)` guard on the final update must never be replaced with a bare `.eq` to preserve concurrency safety; the availability check must always run after pricing validation, never be omitted. | `rental-booking-confirmation.spec.ts`, `rental-booking-deposit-payment.spec.ts`, `admin-pos-v3-booking-deposit-payments.spec.ts` |
| `rental-booking-cancellation.ts` | Customer-facing cancellation: validates ownership and status, evaluates refund eligibility, resolves original payment source, inserts `rental_booking_cancellation_events` + `payment_refunds`, transitions booking to `cancelled`, applies user restriction if excessive cancellations, and best-effort issues cancellation document. | `rental-ops` | `critical` | `resolveOriginalPaymentSource` must throw `ORIGINAL_PAYMENT_SOURCE_NOT_RESOLVED` if no valid paid source is found — never fall back to an unresolved source as the refund row would reference the wrong charge; the cutoff check (`CANCELLATION_REFUND_CUTOFF_PASSED`) must run before any DB write; `assertCustomerCanCancel` must block `"picked_up"` and `"returned"` bookings. | `rental-booking-cancellation.spec.ts` |
| `rental-cancellation-policy.ts` | Pure cancellation policy logic (no DB calls): refund eligibility (3-day Bangkok-TZ cutoff), excessive-cancellation counting (12-month rolling window, threshold >5), user restriction threshold check, and Bangkok timezone conversion helpers. | `rental-ops` | `high` | `EXCESSIVE_CANCELLATION_WINDOW_MONTHS = 12` and `EXCESSIVE_CANCELLATION_ALLOWED_COUNT = 5` are business policy constants — never change without an explicit policy decision; `evaluateBookingDepositRefundEligibility` uses `Asia/Bangkok` TZ and a 3-day cutoff tied to policy version `booking_deposit_refund_calendar_day_v1` — changing either invalidates the versioned policy. | `rental-cancellation-policy.spec.ts` |
| `admin-refunds.ts` | Admin refund management: loads and hydrates `payment_refunds` rows, transitions refund status (`transitionAdminRefund`), links refund proof, and issues the official refund confirmation document — does not initiate gateway refunds (manual transfer only). `"mark-refunded"` delegates its money writes to the atomic RPC `f_mark_rental_booking_refund_refunded` (mig 130): refund flip + FULL held-balance ledger release + residue-0 assertion + `cancel_refund` action-log row in one transaction. | `rental-ops`, `payment`, `money-ledger` | `high` | `"mark-refunded"` must go through the mig-130 RPC — never a direct `status = "refunded"` update; a validated refund proof AND `manual_transfer_reference` are hard requirements BEFORE the RPC call (evidence-before-money, gate 130); the RPC derives the ledger release amount itself — never pass a caller-computed amount; non-money transitions keep the `.eq("status", current)` optimistic guard; `issueRefundConfirmationDocument` only after the RPC commits. | `admin-refunds.spec.ts` |
| `admin-rental-booking-cancel.ts` | Admin cancel wrappers over the mig-129 money RPC: `lateCancelForfeitRentalBooking` (staff, `late_cancel_forfeit` mode, 7-day-v2 window — forfeit DENIED while the customer still self-serves) and `companyCancelRentalBooking` (super_admin, `company_cancel_refund` mode, full refund derived by the RPC; bank details = wrapper contract for captured deposits incl. `paid_confirm_failed`; draft no-money → flip+event only). §F inversion on both: every post-guard refusal (incl. staff `not_super_admin` on company cancel) logs a best-effort DENIED `money_ops_decision_logs` row before throwing; ALLOWED row fail-closed before the RPC; correction row on RPC failure; 127-vocab deposit-action-log rows after commit. | `rental-ops`, `money-ledger` | `high` | Never bypass the decision log (allowed row MUST precede the RPC, fail-closed); never compute money amounts here — the RPC derives from booking/ledger; bank details go to the RPC only, NEVER into the decision log (NO-PII); keep `asUuidOrNull` id purity (malformed ids log `entity_id` null); endpoints stay `requirePlatformAdmin` — never the super-admin-only guard (it would throw before the denial log). | `admin-rental-booking-late-cancel.spec.ts`, `admin-rental-booking-company-cancel.spec.ts` |
| `admin-sale-order-cancel.ts` | Sale-order cancel + refund-settle wrappers over the mig-128 RPCs (`cancelSaleOrder` → `f_cancel_sale_order`; `settleSaleOrderRefund` → `f_settle_sale_order_refund`). §F inversion (inherited super_admin contract for cancel): staff denials logged before 403; allowed rows fail-closed pre-RPC; correction rows on RPC failure; idempotent replay (already-cancelled) short-circuits with NO decision row. Refund amount is RPC-derived; settle requires transfer ref + slip evidence. The ONLY sale cancel path — the raw flip in `admin/orders/[id].patch.ts` is retired (SALE_ORDER_CANCEL_MOVED). | `payment`, `inventory`, `money-ledger` | `high` | Never reintroduce a direct `orders.status='cancelled'` write anywhere; never pass amounts to the RPCs; bank details go to the RPC only, never the decision log (NO-PII); keep the replay short-circuit BEFORE the not-cancellable denial; endpoints stay `requirePlatformAdmin`. | `admin-sale-order-cancel.spec.ts` |
| `mixed-cancel-orchestration.ts` | Mixed post-payment cancel orchestrator (`cancelMixedPostPaymentPair`, B4): resolves the booking+order pair from the session's finalized allocations, runs RENTAL leg first (companyCancelRentalBooking) then SALE leg (cancelSaleOrder) then session flip — wrapper-only, NO cross-domain transaction (each leg's RPC is its own txn). Resume: committed legs verified-and-skipped (stored cancellation reason must equal resubmission, else 409 MIXED_CANCEL_PENDING_MISMATCH); finalized allocations are NEVER voided (money history); §F logging happens inside the per-leg wrappers. | `payment`, `rental-ops`, `money-ledger` | `high` | Never reorder the legs (rental first — no-show cron race, annex #3); never wrap the legs in one transaction; never void finalized allocations; the mismatch check must stay BEFORE any leg executes. | `mixed-cancel-orchestration.spec.ts` |
| `admin-stuck-deposits.ts` | B-M1 recovery surface: `listStuckDepositBookings` (paid_confirm_failed AND still draft/confirmed — cancelled bookings exited via refund; money context incl. live ledger held) and `retryConfirmStuckBooking` (T2 resume shape: stored money state verified FIRST — ledger collection for the booking's exact payment source must exist — then ONLY the confirm half re-runs via confirmRentalBooking with the held-balance-event requirement; on success restores 'paid' + clears failure fields; idempotent; persisting conflicts surface as 409, never force-confirmed). Not money-destructive → no §F operation; the refund exit routes to companyCancelRentalBooking. | `rental-ops`, `money-ledger` | `high` | Retry must NEVER write ledger events or money amounts — money state is verify-only here; never widen the list predicate back to status-blind (cancelled = exited); never force-confirm past a 409 conflict. | `admin-stuck-deposits.spec.ts` |
| `admin-document-void.ts` | Document void + reissue wrappers over the mig-131 RPCs (`voidOfficialDocument` → `f_void_official_document`; `reissueOfficialDocument` → fresh number via `f_next_document_number` + snapshot-clone insert with `original_document_id` + `f_mark_official_document_replaced` flip). §F inversion (operation='document_void', super_admin): staff denials logged before 403, allowed row fail-closed pre-RPC, correction on failure, replay short-circuit (voided/replaced → alreadyVoided, no decision row). Reissue reproduces content byte-for-byte from the immutable snapshot — edits never (§B); idempotent on an existing linked replacement. | `payment`, `money-ledger` | `high` | Never mutate document content — reissue clones the snapshot verbatim; never reuse a voided number (fresh f_next_document_number always); the flip RPC must only run after the replacement row exists with the correct original_document_id link; endpoint stays requirePlatformAdmin. | `admin-document-void.spec.ts` |
| `money-ops-log.ts` | Decision-log writer for money-destructive admin operations (`logMoneyOpsDecision`, mig-132 table): closed operation/entity vocabularies, `failClosed` option (allowed rows) vs best-effort (denied rows). | `payment`, `money-ledger` | `med` | NO-PII invariant: only opaque UUIDs, closed vocab, numeric amounts — never names/phones/bank details/transfer refs/paths/free text; `failClosed: true` for every 'allowed' row gating a money action; denial logging must never block the refusal (best-effort default). | `admin-rental-booking-late-cancel.spec.ts` |
| `rental-booking-launch-cancel.ts` | Launch-era cancellation wrapper (`cancelRentalBookingLaunch`) over `f_cancel_rental_booking_launch` (mig 145), which is the writer authority: it locks the booking, validates the status with named errors, flips it to `cancelled` — which IS the slot release, since availability blocks only `confirmed`/`picked_up` at both the app layer (`rental-booking-availability.ts:22,91`) and the mig-058 trigger — and writes its own §F row atomically. ZERO money branches: no deposit, settlement, ledger or document object is read or written. Enforces booking ownership for the customer path (the RPC cannot distinguish customer-own from staff-any). `launchCancelError` maps each RAISE to HTTP + Thai copy (404 not-found / 409 status+settlement / 422 input / 500 infra), keyed on the leading RAISE token. Serves three surfaces: customer self-serve, staff detail, POS history. | `rental-ops`, `rental-money` | `high` | The customer path MUST pass `requireOwnerUserId` — omitting it lets any authenticated user cancel any booking. NEVER call the 135-gated legacy cancel RPCs from here; this is a parallel lean path, not an un-gate, and deposit revival must stay a config flip. The free-text reason goes to `rental_bookings.cancellation_reason` and NEVER into `money_ops_decision_logs` (NO-PII). Do NOT write the deposit-era `cancellation_refund_*` columns — a launch cancel has no refund policy. The success §F row is written IN-RPC; the wrapper must not add one. | `rental-booking-launch-cancel.spec.ts` |
| `rental-settlement-waive.ts` | Settlement-payment WAIVE wrapper (`waiveSettlementPayment`) over `f_waive_settlement_payment` (mig 141), which is the writer authority: it flips the payment state to `waived`, cancels the pending charge lines, and writes its OWN §F success row atomically (141:86-89). This wrapper owns DENIALS ONLY — it writes a `settlement_payment_waive_denied` / `denied` row to `money_ops_decision_logs` (separate transaction, best-effort) BEFORE returning the error, on its own fast-fail guards AND on every RPC RAISE. `waiveRpcError` maps each RAISE to HTTP + a machine denial code + Thai copy (403 super-admin / 404 not-found / 409 state / 422 input / 500 infra), keyed on the leading RAISE token. | `payment`, `money-ledger`, `rental-money` | `high` | §F INVERSION — the route MUST use `requirePlatformAdmin` with the super_admin check HERE; `requireSuperAdmin` would throw before the denial log and re-open the §8.9 silent-audit gap (pinned by rental-settlement-waive.spec.ts). NEVER log an 'allowed' row — the RPC owns the success path; a wrapper row would double-count. NO-PII: the staff free-text waive reason is passed to the RPC for the domain row and must NEVER enter the decision log (machine codes only). The actor role must come from the session guard, never from the request body — the RPC takes `p_actor_role` as a parameter and does NOT look it up. Denial logging must never block the refusal (best-effort). | `rental-settlement-waive.spec.ts` |
| `rental-booking-availability.ts` | `assertRentalBookingAvailability` queries `rental_bookings` for `confirmed`/`picked_up` rows overlapping a requested range via a half-open interval check and throws `RENTAL_BOOKING_CONFLICT`; `isRentalBookingConflictError` also maps the DB trigger's `23P01` to that conflict. First-line application-layer double-booking guard. | `rental-ops`, `inventory` | `high` | The `.lt("start_date", endDate).gt("end_date", startDate)` pair is the half-open overlap test — reversing either comparator weakens it, but migration 058's `BEFORE INSERT/UPDATE` trigger `rental_bookings_prevent_blocking_overlap` is the atomic backstop, so a defect surfaces as a loud `23P01` conflict rather than silent double-booking; `BLOCKING_RENTAL_STATUSES` (`confirmed`,`picked_up`) must stay aligned with that trigger's blocking set; `isRentalBookingConflictError` must keep matching `23P01` so the DB backstop still maps to a 409. | `rental-booking-availability.spec.ts`, `rental-booking-confirmation.spec.ts` |
| `rental-fulfillment.ts` | `completeRentalBookingFulfillment` (+ `assertRentalFulfillmentPrerequisites`) orchestrates POS pickup and return: loads the booking via `ADMIN_RENTAL_BOOKING_DETAIL_SELECT`, checks branch access (best-effort), enforces pickup gates (deposit paid + KYC gate via `kyc_profiles` + completed checklist), uploads the signature, transitions `confirmed`→`picked_up` / `picked_up`→`returned`, writes KYC audit snapshot to `rental_booking_fulfillments` at pickup time, and at return writes deposit refund fields (`deposit_refund_status`/`amount`, `deposit_payment_status` → `refunded`/`partial_refund`) plus a deposit action-log row. | `rental-ops`, `rental-money`, `branch-access`, `kyc` | `high` | `isMissingBranchAccessTable` must match only table-not-found errors — widening it to swallow general DB errors silently bypasses branch access for all staff; the `.eq("status", requiredStatus)` predicate on every booking UPDATE must not be relaxed; `assertPickupCustomerEvidence` reads `kyc_profiles` (never `users.kyc_status`) with a live `new Date()` — never cache or reuse a prior readiness result (TOCTOU-safe); `resolvePickupKyc` is the only permitted gate function — never inline KYC checks; registered customers resolve via `user_id`, walk-in customers via `rental_bookings.kyc_profile_id` (never by phone); snapshot consistency is application-enforced at INSERT (DB CHECKs removed): `verified`→`kyc_profile_id NOT NULL`, `override`→`kyc_override_id NOT NULL` (matched override, not arbitrary array[0]); KYC snapshot columns must remain NULL for return events; the return refund guard and `assertRefundProof` must run before the refund-state write. | `rental-fulfillment.spec.ts` |
| `rental-return-settlement.ts` | T2 return-settlement ORCHESTRATOR: prerequisites probe (refund-0 payload) → `f_settle_rental_booking_return` RPC (mig 147, 14-arg — writer authority for the settlement row + payment-state + ledger; the RPC alone enforces the memo-when-late rule, the discount tiers by looked-up role, regime channel exclusion, and pending_review, and REFUSES any non-empty staff-charge lines outright. THE WEB COLLECTS ONE MONEY ITEM: `rental_charge`, the rental as booked; `amount_due = rental base - discount`. `late_days` is still derived and stored but is REPORT DATA, not money — no late charge is computed anywhere) → legacy refund-proof bridge row → UNCHANGED `completeRentalBookingFulfillment` with the SCOPED refund mapping (capped at `deposit_paid_amount`, §b addendum item 7). Passes the launch channel through to the RPC (rental-base discount + note; the RECORD-BUT-NO-MONEY `staffMemo` — text only, no amount, no charge line, no effect on any total) and surfaces the launch return keys (`discountAmount`/`rentalBase`). It sends NO charge lines at all — the staff-charge channel is gone from the input, the result and the RPC call, so the RPC's default (empty) applies. Also exports the PURE `buildLaunchSettlementPreview` — the booked rental plus the OVERDUE DAYS AS A FACT (Asia/Bangkok, end_date EXCLUSIVE); it computes NO late charge, because the web collects ONE money item and overdue rental is billed externally (decisions.md 2026-07-27). Used by the GET feed for DISPLAY ONLY. `settleReturnRpcError` maps every RPC RAISE to an HTTP status + Thai user message (409 regime / 403 discount-tier / 422 business / 500 infra), keyed on the leading RAISE token; the endpoint re-uses it. Crash-safe resume when a settlement exists and the booking is still `picked_up` (intent-match on the deposit-era figures, 409 SETTLEMENT_PENDING_MISMATCH on drift, audit row per resume; launch totals not recomputed on resume). | `rental-money`, `rental-ops` | `high` | NEVER call the RPC before the prerequisites probe (stranded-state guard); the probe payload must stay refund-0/not_applicable; fulfillment stays byte-unchanged — compose via its public API only (pinned by rental-return-settlement.spec.ts); RPC args must pass explicit nulls, never undefined (PostgREST named-arg matching); the util does SHAPE/SERIALIZATION only — it must NOT re-validate the tier or the memo-when-late rule (the RPC is the sole money authority), and must never derive late days itself; `settleReturnRpcError` must key on the leading RAISE token and default unknown codes to 500; resume must never proceed on figure mismatch. | `rental-return-settlement.spec.ts` |
| `rental-pickup-readiness.ts` | Loads and builds pickup readiness for a rental booking: booking-status gate, branch context, KYC gate (reads `kyc_profiles` by `user_id` for registered customers or by `rental_bookings.kyc_profile_id` for walk-in customers + `kyc_pickup_overrides` by `booking_id`), money summary, date warnings. Pure `buildRentalPickupReadiness` for unit testing; `loadRentalPickupReadiness` for DB reads; `assertPickupReadinessBranchAccess` for branch auth. | `rental-ops`, `kyc` | `high` | KYC gate is driven exclusively by `kyc_profiles` — never fall back to `users.kyc_status`; `resolvePickupKyc` is the only permitted gate function — never inline KYC checks; registered customers: query `kyc_profiles` by `user_id` only; walk-in customers: query by `rental_bookings.kyc_profile_id` only — never by `walk_in_phone`; `buildRentalPickupReadiness` must stay a pure function (no DB calls). | `rental-pickup-readiness.spec.ts` |
| `rental-booking-no-show.ts` | `markRentalBookingNoShow` marks a `confirmed` booking `no_show` once the Bangkok pickup date has passed: inserts a `rental_booking_no_show_events` row, a forfeiture `rental_booking_deposit_disposition_events` row, and a `financial_recognition_events` (`booking_deposit_forfeiture_income`) ledger row, then updates the booking to `no_show` with `deposit_refund_status = "forfeited"`, `deposit_refund_amount = 0`; rolls back the inserts if the final guarded UPDATE fails. | `rental-ops`, `rental-money` | `high` | Rollback must delete in FK-safe order (`financial_recognition_events` → `rental_booking_deposit_disposition_events` → no-show event) — wrong order leaves orphaned ledger rows; `assertBookingCanBeMarkedNoShow` must compare via `toBangkokLocalDate` (Asia/Bangkok) — a wrong TZ would forfeit/recognise income on or before the pickup date; the final UPDATE's `.eq("status", "confirmed")` guard must stay to block double-forfeiture (the per-booking unique indexes on the event/disposition/recognition rows are the idempotency backstop). | `rental-booking-no-show.spec.ts` |
| `admin-pos.ts` | POS catalog SELECT strings (`ADMIN_POS_ASSET_SELECT`, `ADMIN_POS_SALE_SKU_SELECT`) and pure mappers/helpers (`mapPosAssetCatalogItem`, `mapPosSaleCatalogItem`, `buildPosAssetSnapshot`, `isRentablePosAsset`) that shape asset/sale-SKU rows for the POS UI — no DB calls. | `pos`, `other` | `low` | — | `admin-pos.spec.ts`, `admin-pos-api.spec.ts` |
| `admin-branches.ts` | Store-branch admin helpers: `ADMIN_STORE_BRANCH_SELECT`, `mapAdminStoreBranchItem`, `buildBranchPayload` (trims/whitelists input), and `resolveActiveStoreBranch` (read-only lookup that throws 422 if the branch is missing or inactive) — no money or access-control writes. | `other` | `low` | — | `public-branches-api.spec.ts` |
| `admin-partner-categories.ts` | Partner taxonomy helpers (migrations 116 + 117): `ADMIN_PARTNER_CATEGORY_SELECT`, `ADMIN_PARTNER_CATEGORY_ASSIGNMENT_SELECT`, `mapAdminPartnerCategory`, `mapAdminPartnerCategoryAssignment`, `replacePartnerCategoryAssignments` (validates category IDs incl. level/parent rules, deletes existing assignments, inserts new set — no money writes), `fetchPublicTaxonomyForPartners` (display-only read for public routes — groups active/public taxonomy by partner, primary-first; no ids exposed), and `resolvePartnerIdsForTaxonomy` + `sanitiseTaxonomySlugParam` (public /partners taxCategory/taxSubcategory filter: resolves slugs → matching partner ids; sub matches sub-OR-parent; unknown/inactive slug → empty). | `other` | `low` | `replacePartnerCategoryAssignments` is a delete-then-insert (non-transactional); validate all IDs (existence, active, primary level-0, secondaries level-1 + child of primary) before delete to avoid clearing assignments on bad input; never add money/ledger/payment fields to the insert payload; `fetchPublicTaxonomyForPartners` runs under service-role (RLS bypassed) so it MUST keep the explicit `is_active AND is_public` category filter, and callers must pass only `is_public` partner ids (partner-visibility is route-enforced, not checked here); `resolvePartnerIdsForTaxonomy` likewise runs under service-role and does NOT gate partner visibility — it returns ids for matching assignments regardless of `is_public`, so the calling route MUST keep its own `.eq("is_public", true)` gate (null return = no filter, `[]` = empty result — never treat `[]` as "no filter"). | `partner-category-assignments.spec.ts` |
| `admin-orders.ts` | Admin order/booking utility hub: SELECT strings (incl. `ADMIN_RENTAL_BOOKING_DETAIL_SELECT` consumed by write-path callers), row mappers, filter parsing, `fetchAdminRentalBookingListRows` (schema-fallback fetcher), `fetchAdminUserProfiles`/`fetchAdminCustomerProfile`, action-required helpers (`countAdminActionRequiredItems`), and re-exported status-transition allowlists — no money writes. | `other` | `med` | `ADMIN_RENTAL_BOOKING_DETAIL_SELECT` is consumed by `rental-fulfillment.ts` and `rental-booking-no-show.ts` — dropping a column gives those callers a silent `null` field instead of a PostgREST error, so keep every consumed column; `fetchAdminRentalBookingListRows` must try all fallback SELECTs before throwing — never short-circuit, or older-schema deployments break. | `admin-orders.spec.ts` |
| `admin-order-queue.ts` | Admin sale-order queue view: `parseAdminSaleOrderQueueFilters`, `ADMIN_SALE_ORDER_QUEUE_SELECT`, a multi-view count summary, and `fetchAdminSaleOrderQueue` (paginated, view-filtered, with branch/profile hydration) — all read-only DB operations / pure shaping. | `other` | `low` | — | `admin-order-queue.spec.ts` |
| `rental-deposit-slip-evidence.ts` | Private bank-transfer deposit-slip evidence boundary for the `rental-deposit-slips` bucket: magic-byte MIME sniff (`sniffRentalDepositSlipMime` — JPEG/PNG/PDF only, client MIME never trusted), 10 MB cap, opaque storage key (`booking-deposit-slips/<bookingId>/<random-uuid>.<ext>`), private upload + `rental_booking_deposit_slips` metadata insert (status `pending_review`), short-lived signed-URL helper (`createRentalDepositSlipSignedUrl`), and the safe mapper/SELECT (never storage path/bucket/URL). Never mutates booking/money. | `rental-ops` | `high` | Files must live ONLY in the private `rental-deposit-slips` bucket — never catalog-media, never `getPublicUrl`, never a permanent public URL; the safe mapper/SELECT must never expose `storage_path`, `storage_bucket`, or any URL; this module must never mutate booking status, deposit/payment fields, payment lines, or held-balance events, and never confirm a booking; canonical MIME comes from magic-byte sniff only — never the client-declared MIME. | `rental-deposit-slip-evidence.spec.ts`, `user-rental-deposit-slip-api.spec.ts`, `admin-rental-deposit-slips-api.spec.ts` |
| `rental-manual-deposit-confirmation.ts` | Admin manual bank-transfer booking-deposit confirmation: delegates the ATOMIC money core + confirm to the `f_confirm_rental_booking_deposit` RPC (migration 119, source_type `manual_admin_confirmation`, source_id = bookingId, `p_attempt_id` null), maps its return / RAISE codes, then marks the reviewed slip OUTSIDE the money txn (best-effort). Amount is server-guarded: must equal `calculateBookingDepositDueNow` (3-tier formula, same as POS V3) or 422 `BOOKING_DEPOSIT_AMOUNT_MISMATCH` — advance bookings only; client amount is never trusted. No revenue/VAT, no Omise/payment_attempts. | `rental-money`, `rental-ops` | `critical` | Confirmation goes ONLY through the migration-119 RPC — never set `rental_bookings.status` directly and never re-introduce separate held-balance/deposit writes; the deposit is a held-balance LIABILITY — never write it as revenue, never into `rental_booking_payment_lines`, never trigger VAT; the held-balance event stays idempotent (`source_id = bookingId`); slip-review stays outside the money txn; do not remove or widen the formula amount guard (owner decision 2026-07-10). | `rental-manual-deposit-confirmation.spec.ts`, `rental-held-balance-events.spec.ts`, `rental-booking-confirmation.spec.ts`, `migration-119-atomic-deposit-confirm.spec.ts` |
| `customer-rental-booking-detail.ts` | CUSTOMER-FACING rental read surface and the customer document rails. `loadCustomerRentalBookingRow` is the ownership gate — it 403s when `rental_bookings.user_id` is not the caller — and every other export composes on it. `getCustomerRentalBookingDetail` assembles the customer payload: booking identity/status, the money block (deposit paid/due, rental fee, security-deposit total), latest payment attempt, cancellation + refund-request + refund-proof state, eligibility, the customer-safe document set, and the [146] RECORD-BUT-NO-MONEY `returnMemo` (settlement `staff_memo` + `late_days`, TEXT ONLY — no amount, no charge, nothing owed; null when absent), plus `depositsEnabled` — the deposit regime read through `f_deposits_enabled()` (mig 135), FAIL-CLOSED (missing rpc capability or any error resolves FALSE), which the customer surfaces use to gate deposit DISPLAY. `issueCustomerRentalDocument` self-issues only `CUSTOMER_SAFE_DOCUMENT_TYPES`; `getCustomerRefundProofAccess` / `listCustomerRefundProofStatuses` gate refund-proof reads on the same ownership rule. | `rental-money`, `documents` | `high` | NEVER return a field the customer may not see: every read goes through `loadCustomerRentalBookingRow`'s 403 (ownership is application-enforced — these routes use the service role, so RLS is not a backstop). The document self-issue path must stay restricted to `CUSTOMER_SAFE_DOCUMENT_TYPES`. Deposit fields are DATA, not display authority — a customer money surface may render them only when a deposit exists (decisions.md 2026-07-26 addendum, deposit DATA vs deposit DISPLAY). `returnMemo` is staff free text shown verbatim: never sum it, never format it as money, never present it as an obligation. | `customer-rental-booking-detail.spec.ts`, `rental-return-settlement.spec.ts` |
| `customer-rental-booking-draft.ts` | Authoritative server-side creation of a customer rental booking `draft` (`createCustomerRentalBookingDraft`): validates the asset (active + not hidden), validates dates (Bangkok-local, min/max rental days), computes pricing from the asset via `decomposeRentalDuration`, and inserts a `draft` `rental_bookings` row with `asset_id` set. Replaces the former client-side direct PostgREST insert (which sent `asset_id: null` → CHECK `rental_bookings_root_chk` 400). Evidence-only — the row stays `draft`. | `rental-ops`, `rental-money` | `med` | `asset_id` MUST be set from the validated asset row (satisfies CHECK `rental_bookings_root_chk`); pricing (`daily_rate`/`rental_total`/`deposit_amount`) is computed from the asset — never trust client amounts; `user_id` comes from the caller's session, never the request body; the row must stay `status='draft'` with `walk_in_phone=null` (no confirmation, no held-balance, no inventory). | `customer-rental-booking-draft.spec.ts` |
| `sale-order-payment-slip-evidence.ts` | Private bank-transfer SALE ORDER payment-slip evidence boundary for the `sale-order-payment-slips` bucket: magic-byte MIME sniff (JPEG/PNG/PDF; client MIME never trusted), 10 MB cap, opaque storage key (`order-payment-slips/<orderId>/<random-uuid>.<ext>`), private upload + `sale_order_payment_slips` metadata insert (status `pending_review`), short-lived signed-URL helper, and the safe mapper/SELECT (never storage path/bucket/URL). Separate from the rental deposit-slip flow; never marks the order paid. | `other` | `high` | Files must live ONLY in the private `sale-order-payment-slips` bucket — never catalog-media, never `getPublicUrl`, never a permanent public URL; the safe mapper/SELECT must never expose `storage_path`/`storage_bucket`/any URL; this module must NEVER mark the order paid, deduct inventory, write any rental ledger (`rental_held_balance_events`), or reuse `rental_booking_deposit_slips`; canonical MIME comes from magic-byte sniff only. | `sale-order-payment-slip-evidence.spec.ts`, `user-sale-order-payment-slip-api.spec.ts`, `admin-sale-order-payment-api.spec.ts` |
| `sale-order-manual-payment.ts` | Admin manual bank-transfer SALE ORDER payment confirmation: marks `orders.payment_status='paid'` + `status='confirmed'` (existing paid-write convention, guarded → idempotent), deducts inventory via the existing idempotent `f_apply_order_inventory` RPC, clears the buyer cart (best-effort), and optionally marks the reviewed slip. Reuses the existing safe order paid path; invents no VAT/revenue (none exists for sale orders). | `payment` | `critical` | Reuse the EXISTING paid path — `payment_status='paid'`/`status='confirmed'` guarded by `.neq('payment_status','paid')` for idempotency; inventory MUST go through the idempotent `f_apply_order_inventory` RPC (never a hand-rolled deduction); NEVER invent VAT/revenue logic (the sale-order model has none); NEVER touch `rental_held_balance_events`, `confirmRentalBooking`, or Omise/`payment_attempts`; a replay on an already-paid order must no-op. | `admin-sale-order-payment-api.spec.ts` |
| `manual-payment-request.ts` | Central manual payment-request domain logic: builds/reuses one `manual_payment_requests` row (+ `manual_payment_request_items`) from the cart's unpaid sale order and/or draft booking targets with authoritative server-computed amounts (`createManualPaymentRequestFromTargets`); owner-scoped detail assembly + list; and admin EVIDENCE-only `reviewManualPaymentRequest` / `rejectManualPaymentRequest` (status only). Allocation total = sum of item amounts; booking deposit via `calculateBookingDepositDueNow`. | `payment` | `high` | EVIDENCE ONLY — this module must NEVER mark an order `paid`, confirm a booking, deduct inventory, write a `rental_held_balance_events` row, or touch Omise/KYC; create enforces ownership + order `awaiting_payment`/`pending_review` + booking `draft` before any write; amounts are computed from the targets, never trusted from the client; review/reject only advance `manual_payment_requests`/`manual_payment_request_slips` status — admin still confirms sale/booking via the existing actions. | `manual-payment-request-api.spec.ts`, `manual-payment-request-create-api.spec.ts`, `admin-manual-payment-request-api.spec.ts` |
| `manual-payment-request-slip-evidence.ts` | Private bank-transfer slip evidence boundary for the central `manual-payment-slips` bucket: magic-byte MIME sniff (JPEG/PNG/PDF only, client MIME never trusted; rejects SVG/fake-MIME), 10 MB cap, sha256 hash, opaque storage key (`payment-request-slips/<requestId>/<random-uuid>.<ext>`), private upload + `manual_payment_request_slips` metadata insert (status `pending_review`), short-lived signed-URL helper, and the safe mapper/SELECT (never storage path/bucket/URL). | `payment` | `high` | Files must live ONLY in the private `manual-payment-slips` bucket — never catalog-media, never `getPublicUrl`, never a permanent public URL; the safe mapper/SELECT must never expose `storage_path`/`storage_bucket`/any URL; this module must NEVER mark an order paid, confirm a booking, deduct inventory, or write any rental ledger; canonical MIME comes from magic-byte sniff only. | `manual-payment-request-slip-evidence.spec.ts`, `manual-payment-request-api.spec.ts` |
