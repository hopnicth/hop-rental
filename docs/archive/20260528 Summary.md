# 20260528 Project Summary

## ภาพรวมสั้น ๆ

HOP-RENTAL คือระบบหลักของ HOPNIC ที่รวม **ขายสินค้า**, **เช่าสินทรัพย์/อุปกรณ์**, **บริการ/คอนเทนต์**, **พื้นที่ลูกค้า**, และ **หลังบ้านแอดมิน/หน้าร้าน POS** ไว้ในแอปเดียว

โปรเจกต์นี้ไม่ได้เป็นแค่เว็บขายของ แต่เป็นระบบปฏิบัติการสำหรับงาน commerce + rental + admin operations

---

## ระบบนี้ทำอะไรได้บ้าง

### 1) ฝั่งลูกค้า / Storefront
- ดูสินค้าขายขาด
- ดูสินทรัพย์ให้เช่าแบบ asset-first
- ดูบริการ, บทความ, รีวิว, โปรโมชั่น, พาร์ทเนอร์
- ค้นหาแบบรวมหลายประเภทผ่าน `/search`
- ใช้งานหลายภาษา: `th`, `en`, `cn`, `jp`

### 2) ฝั่งลูกค้า / Self-service
- สมัคร/เข้าสู่ระบบด้วย Supabase Auth
- จัดการบัญชีผู้ใช้
- ดูตะกร้าเดียวสำหรับ sale + rental draft
- ดูประวัติคำสั่งซื้อ
- ดูประวัติ/รายละเอียดการเช่า
- บันทึก wishlist / save list / saved partners
- มี KYC / company / tax profile / เอกสารบางส่วนในฝั่ง user

### 3) ฝั่งหลังบ้าน / Admin
- จัดการสินค้า
- จัดการ asset สำหรับเช่า
- จัดการสต๊อกและ branch inventory
- จัดการ sale orders
- จัดการ rental bookings
- จัดการ refunds
- จัดการ content pages: blog / service / promotion / review
- จัดการ home content, home categories, partner data
- มี admin inbox สำหรับแชทลูกค้า
- มี POS สำหรับงานขายและงานเช่า

---

## Tech Stack

- **Frontend:** Nuxt 4 + Vue 3 + TypeScript
- **UI:** `@nuxt/ui`
- **Auth / DB / Storage / Realtime:** Supabase
- **i18n:** `@nuxtjs/i18n` (`th`, `en`, `cn`, `jp`)
- **Editor/CMS content:** TipTap
- **Payments:** Omise integration
- **Testing:** Vitest

โครงสร้าง state หลักใช้แนว `composables + refs` ยังไม่ได้ใช้ Pinia/Vuex เป็นแกนหลัก

---

## โครงสร้างโปรเจกต์แบบเข้าใจง่าย

### `app/`
ส่วนหน้าเว็บทั้งหมด
- `pages/` route หลักของระบบ
- `components/` UI components
- `composables/` business logic ฝั่ง client
- `types/` types กลาง
- `utils/` utility + domain logic
- `layouts/` default/admin layouts
- `plugins/` เช่น locale persistence

### `server/api/`
API ฝั่ง Nitro สำหรับ public, user, admin, payment, webhook, chat

### `supabase/`
schema migration, SQL evolution, storage/database behavior

### `tests/server/`
ชุด test สำหรับ business rules, API, UI contracts, POS, orders, chat, refunds, partners ฯลฯ

### `i18n/locales/`
ไฟล์แปลภาษา 4 ภาษา

---

## เส้นทางหลักของระบบ

### Public / Customer-facing
- `/`
- `/product-{group}`
- `/product-rental`
- `/asset/[slug]`
- `/services`, `/blog`, `/promotions`, `/reviews`
- `/partners`
- `/search`

### User
- `/user/login`
- `/user/cart`
- `/user/orders`
- `/user/rentals`
- `/user/account`
- `/user/wishlist`
- `/user/save-list`

### Admin
- `/admin/products`
- `/admin/assets`
- `/admin/branches-inventory`
- `/admin/orders`
- `/admin/rental-bookings/[id]`
- `/admin/pos`
- `/admin/pos-v3`
- `/admin/refunds`
- `/admin/content`
- `/admin/home-content`
- `/admin/home-categories`
- `/admin/partners`
- `/admin/messages`

---

## แกนธุรกิจสำคัญของโปรเจกต์

### A. Sale Commerce
- ลูกค้าเลือกสินค้า ใส่ cart และ checkout
- มี order history และหน้า payment/result
- แอดมินจัดการคำสั่งซื้อจาก queue และ order detail

### B. Rental Commerce
- ระบบเช่าเน้น **asset-first** ไม่ใช่ product-first
- ลูกค้าสร้าง booking draft แล้วค่อยยืนยันผ่าน cart
- POS สามารถสร้าง confirmed rental ได้โดยตรงสำหรับลูกค้าทั่วไปหรือ walk-in
- มีสถานะงานเช่า เช่น `draft`, `confirmed`, `picked_up`, `returned`, `cancelled`, `no_show`

### C. Content + Service + Partner Layer
- มี CMS สำหรับ blog / service / promotion / review
- service pages ผูกข้อมูลผู้ให้บริการและช่องทางติดต่อได้
- partners เป็นอีกกลุ่มข้อมูลแยกสำหรับ directory/store/service/contractor
- หน้า home ใช้ข้อมูลจากฐานข้อมูลจริง ไม่ใช่ mock อย่างเดียว

### D. Internal Operations
- มี branch-based inventory
- มี admin POS สำหรับขายและเช่า
- มี refund workflow
- มี customer support chat
- มี document / booking / payment related operational flows หลายส่วนแล้ว

---

## Business Rules ที่ทีมควรรู้

- ระบบ admin ใช้ `public.users.platform_role` เป็นตัวตัดสิทธิ์หลัก
- rental flow เป็น **asset-first**
- booking cancellation เป็นแนว soft-delete โดยใช้ status
- rental booking ต้องมี `user_id` หรือ `walk_in_phone`
- POS sale ไม่บังคับข้อมูลลูกค้าเสมอไป แต่ POS rental ต้องรู้ตัวลูกค้าหรือ walk-in
- ระบบรองรับหลายภาษา และค่าเริ่มต้นคือภาษาไทย
- public content และ homepage หลายส่วนถูกดึงจาก DB/CMS แล้ว
- search เป็น multi-type search ครอบคลุม product, rental, service, review, blog, promotion

---

## จุดที่ถือว่า mature แล้ว

- Product / Asset / Inventory / Order / Content admin มีโครงสร้างใช้งานจริงค่อนข้างครบ
- Universal Search มีทิศทางชัดและใช้งานได้จริง
- Home content และ category cards เป็น DB-backed
- Chat, refunds, partner listing, content pages, multi-language มีฐานระบบแล้ว
- Test suite ฝั่ง server ค่อนข้างเยอะ แปลว่าโปรเจกต์มีการล็อก behavior สำคัญไว้พอสมควร

---

## จุดที่ยังต้องตามต่อ / ยังไม่ปิดเต็ม

- POS V3 ยังเป็นช่วงวาง foundation และยังมี scope ที่ต้อง reconcile อีกมาก
- เอกสาร/receipt/tax document บางสายงานยังไม่ครบทั้งหมด
- booking deposit / forfeiture / refund บางช่วงยังเป็น phased rollout
- search และ filter บางชิ้นยังมีงาน schema/data alignment ต่อ
- content classification และ main category backfill ยังเป็นงานที่ควรตามเก็บ

---

## ถ้าจะอธิบายให้ทีมแบบสั้นมาก

> HOP-RENTAL คือแพลตฟอร์ม Nuxt + Supabase ที่รวม storefront, rental booking, customer self-service, CMS content, partner directory, admin operations, refund flow, chat support และ POS ไว้ในระบบเดียว โดยมีโครงสร้างหลายภาษาและ database-driven content ค่อนข้างครบแล้ว ส่วนงานที่ยังต้องเดินต่อหลัก ๆ คือ POS V3, เอกสารทางการบางประเภท, และการเก็บรายละเอียดของ flow เงินมัดจำ/คืนเงินในบางกรณี

---

## เอกสารที่ทีมควรอ่านต่อ

1. `README.md`
2. `map.md`
3. `PROJECT_SUMMARY.md`
4. `API_INDEX.md`
5. `ROLE_MATRIX.md`
6. `ADMIN_MVP_ACTION_PLAN.md`
7. `ASSET_ACTION_PLAN.md`
8. `docs/booking-deposit-forfeiture-accounting-document-design.md`

---

## หมายเหตุ

สรุปฉบับนี้ทำเพื่อใช้เล่าให้ทีมเข้าใจภาพรวมเร็ว ๆ ณ วันที่ `2026-05-28`
ถ้าจะ onboard dev ใหม่ แนะนำให้อ่าน `map.md` และ `PROJECT_SUMMARY.md` ต่อทันที