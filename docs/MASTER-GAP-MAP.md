# MASTER GAP MAP — T1–T8 routing table

Status: **ACTIVE routing table** (not a report). One page. Source: the three journey audits
(purchase `8886ddb`, rental `2db625a`, mixed `84857de`), the B6 staff/POS walk
(`docs/audit/2026-07-10-b6-walkthrough.md`), and `docs/BACKLOG.md`. Ordering + policy are locked in
`decisions.md` "2026-07-14 — Post-journey-audit decision set (owner)". This table supersedes the
older V3-0→V3-6 slice track as the delivery routing (V3 slices now map into the T-tracks below).

**Delivery unit = one flow that walks end-to-end. Acceptance = a re-walk shows the referenced audit
mark IDs resolved.** Reusable-by-evidence: anything proven duplicated across ≥2 flows AND
money/document-related is built centrally, immediately.

| Track | Scope | Depends on | Finding refs (audit mark IDs) | Decision |
|---|---|---|---|---|
| **T1a** | ✅ **RESOLVED** at `75084ed` — Case-2 B6 / Case-3 Y5 closed (first picked_up booking recorded). Was: Staff-KYC capture + SUPER ADMIN approve queue; link via User-ID QR | — | Case-2 **B6** (pickup 422 `no_profile`), Case-3 **Y5** | §a |
| **T1b** | Customer online KYC self-serve → approve queue — individual id-card path shipped in `75084ed`; REMAINS OPEN: juristic self-serve + users.kyc_status mirror retirement + display unification (BACKLOG, T1a close-out) | T1a ✅ | Case-2 B6/L1 (0 bookings ever picked_up) | §a |
| **T2** | ✅ **RESOLVED** (local; remote apply pending) — L1 release events live (`settlement_application`/`refund`/`forfeiture`/`settlement_additional_collection` all fired with correct amounts, per-booking residue 0), B9 return walks end-to-end (REFUND + COLLECT branches), no-show manual+auto (pg_cron 00:00 Bangkok, system actor). Migrations 125-126. NOT covered: settlement numbered DOCUMENT → T4; pos.vue refund prefill + slip hygiene → T5; deposit-action-log vocabulary → next migration touching it. Was: Pickup → return → settlement = the rental ledger release side | T1 ✅ | Case-2 furthest-reachable dead-end, **L1** (`settlement_application`/`refund`/`forfeiture` = 0 events ever), B9 unreachable; B6 walk (BDC/held-balance) | §b (early-return-in-full, no-show auto-forfeit) |
| **T3** | ✅ **RESOLVED** (local; remote apply pending) — Phase 1 of the 2026-07-19 blueprint delivered: B7 (self-cancel v2 7-day tier + mark-refunded full ledger release), late-cancel forfeiture (ruling 2, pipeline identical to no-show), company cancel (super_admin, §F inversion), D5/BUGS 1-3 (sale cancel RPC + stock restore + raw-flip retirement), B4 (mixed orchestration), B-M1 (stuck surface + retry-confirm/refund exits), cron-race ordering proven live. Migrations 127-132. 8-walk plan + HTTP-debt walk closed. Was: Unified **void / cancel / deposit-refund** path (sale + rental + mixed) | T2 ✅ | Case-1 **D5/BUGS 1-3**, Case-2 **B7**, Case-3 **B4**, B-M1/G4, paid_confirm_failed recovery — all closed by walks 1-8 | §b (refund = bank transfer + reason + bank acct + admin slip); design doc 2026-07-19 |
| **T4** | **T4-core ✅ DELIVERED with T3** (numbering-engine usage, void=reissue chain live [mig 131], cancel/refund/forfeiture document types reused/issued, money-ops decision log [mig 132]). **T4-rest OPEN** (Phase 2): central `tax_treatment` (line×customer→vat/wht), sale receipt + tax invoice [A8/A9], WHT/50-bis, 3 reconciliation loops, settlement numbered document | T4-core ✅; T4-rest designed | Case-1 **A8/A9** (still open — Phase 2), Case-2 **B5/W8**, Case-3 no-docs | §c, §d; design doc 2026-07-19 Phase-1/2 boundary |
| **T-LAUNCH** | **Minimal-launch rework (decisions.md 2026-07-22 minimal-launch entry)**: feature-gate ALL deposits OFF (booking + security + forfeiture pipeline parked, not deleted); single money event at RETURN (return-time charging incl. late-return days + standard service charges); STM-/TIR-/credit-note document types on the T4-core engine (statement → tax invoice/receipt every transaction, full + abbreviated behind config, §86/10 credit-note linkage); conditional address intake (full form only); `awaiting_payment` tax-point guard (engine refuses tax docs before payment confirmed; invoice dated payment date); sales-VAT report export; cancellation = slot release. Own Phase 0 + design pass (fresh terminal). | T4-core ✅ | decisions.md 2026-07-22 (minimal launch); research trilogy docs/research/2026-07-22-* | 2026-07-22 minimal launch |
| **T5** | POS V3 sale + fiscal docs; retire v1 (public-bucket hygiene) — **RE-SCOPED 2026-07-22**: POS fiscal docs ride the T-LAUNCH STM-/TIR- chain; deposit-related POS surfaces follow the deposit-revival track, not T5 | T4, T-LAUNCH | POS-V3 deep audit (`2026-07-09-pos-v3-deep-audit.md`); POS v1 public-bucket slip hygiene | 2026-07-09 §3 (V3-only) |
| **T6** | Cross-surface consistency + status visibility | — (parallelizable) | Case-1 **A7** (tracking on list not detail), Case-1 **#5** (no under-review/rejected signal), Case-2 **B2** (deposit-unpaid badge), **B3/G5** (end-date incl/excl), **W5** (no work queues), Case-3 **W3/W1** (no unified transaction view), **W7** (request stays pending_review) | — |
| **T7** | Glossary / i18n / money-copy | — (parallelizable) | Case-1 **M2** + Case-2 **W2** (template wording leaks), bare มัดจำ (Case-1/2/3 W1), Case-2 **W3** (raw enum), 2026-07-09 §4 (POS V3 Thai) | 2026-07-07 glossary; 2026-07-09 §4 |
| **T8** | Accounting-office export pack — **end goal** (VAT register, WHT register, deposit-liability ledger, cash reconciliation) | T4 | §c reconciliation loops; BACKLOG "Accounting-office export pack" | §c |

## Dependency notes
- **T2 after T1** — T1a satisfied at `75084ed` (B6 422 gate cleared): **T2 unblocked for scheduling**. T1b remains open (juristic self-serve + kyc_status mirror retirement + display unification, per BACKLOG).
- **T3 deposit-refund completeness depends on T2** — SATISFIED: settlement/release machinery live (mig 125-126). T3 unblocked for scheduling (design WITH T4 per the locked pairing).
- **T4 void chain designed together with T3** — cancel/void must emit the correcting document in the same design pass (void = reissue, never edit).
- **T5/T8 depend on T4** (fiscal docs and export registers ride the document/tax foundation).
- **T6/T7 are parallelizable** — no money/schema dependency; fold fixes into whichever flow re-walks that surface.
- **T-LAUNCH re-scopes Phase 2 / T5** (2026-07-22 minimal-launch entry) — deposits feature-gated off; the money flow collapses to one taxable event at return. T-LAUNCH rides the T4-core document engine (numbering, void=reissue, immutability) and needs its own Phase 0 + design pass in a fresh terminal.
- **Deposit revival = FUTURE track (not scheduled)** — the parked deposit/forfeiture machinery (incl. the 2026-07-22 Model-B conversion flow) revives only behind TWO gates: (1) card-hold / authorization-hold capability, (2) the ป.73/2541 professional memo (research report 2). Juristic + WHT parked with it.

## Cross-cutting / infra (not a delivery flow — fold into the touching track)
Dev-default-targets-REMOTE invert (safety) · RLS hardening bundle (mig-070 draft-insert + confirmBooking dead path + legacy upsert) · empty-string `<USelect>` sites · stale-`finalizing` POS sweep/alert · baht-vs-satang decision audit (auditor-owned) · PDPA retention/purge (vs mig-118 delete blocks).
