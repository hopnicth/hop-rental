export default defineAppConfig({
  ui: {
    primary: "green", // กำหนดสีหลักของระบบตาม CI ของคุณ
    gray: "cool",
    notifications: {
      // กำหนดตำแหน่งแสดงผล (เช่น มุมขวาล่าง)
      position: "top-right",
    },
  },
});
